<UserControl x:Class="BOXR.UI.Views.DogProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BOXR.UI.Views"
             mc:Ignorable="d" 
d:DesignHeight="750" d:DesignWidth="1000" Background="Transparent">
    <Grid Margin="20, 10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <TextBlock Text="{Binding Dog.Name}" FontSize="20" Margin=" 0,0,0,20" Padding="5" Grid.Column="0" Grid.Row="0"/>

        <StackPanel Grid.Column="0" Grid.Row="1">
            <TextBlock Text="Pedigree Number" FontWeight="Bold" Padding="5,0"/>
            <TextBlock Text="{Binding Dog.PedigreeNumber}" FontSize="16" Padding="5, 5, 5, 15"/>
            <TextBlock Text="Breeder" FontWeight="Bold" Padding="5,0"/>
            <TextBlock Text="{Binding Dog.Breeder}" FontSize="16" Padding="5, 5, 5, 15"/>
        </StackPanel>
    </Grid>
</UserControl>

